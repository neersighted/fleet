- name: msmtp | install msmtp (apt)
  tags: [msmtp, mail, common]
  with_items:
    - msmtp
    - msmtp-mta
  apt: pkg={{ item }} state=latest
  when: ansible_pkg_mgr == 'apt'

- name: msmtp | configure msmtp
  tags: [msmtp, mail, common]
  template: src=msmtp/msmtprc dest=/etc/msmtprc
