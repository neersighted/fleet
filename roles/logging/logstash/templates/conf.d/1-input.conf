input {
  # Beaver log shipper.
  redis {
    type => 'other'
    key => 'logstash:beaver'
    data_type => 'list'
  }

  # Stash53 dns query shipper.
  redis {
    type => 'dns'
    key => 'logstash:stash53'
    data_type => 'list'
  }
}
