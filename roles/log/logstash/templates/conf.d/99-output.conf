output {
  # Log storage.
  elasticsearch {
    cluster => "{{ cluster }}"
    node_name => "logstash.{{ inventory_hostname }}"
    bind_host => "{{ ipv4.private }}"
  }


  # Debugging.
  stdout {
    codec => rubydebug
  }
}
