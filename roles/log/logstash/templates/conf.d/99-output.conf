output {
  # Log storage.
  elasticsearch {
    host => "localhost"
    cluster => "{{ cluster }}"
    node_name => "logstash.{{ inventory_hostname }}"
    bind_host => "{{ network.ipv4.private }}"
  }


  # Debugging.
  stdout {
    codec => rubydebug
  }
}
