node:
  name: {{ inventory_hostname }}
  data: true
  master: true
cluster:
  name: {{ cluster }}

network:
  bind_host: 0.0.0.0
  publish_host: {{ network.ipv4.private }}
discovery.zen.ping:
  multicast.address: {{ network.ipv4.private }}

# Lock all memory.
bootstrap.mlockall: true
