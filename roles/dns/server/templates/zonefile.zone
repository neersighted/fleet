; GLOBALS
$ORIGIN {{ item.key }}.
$TTL {{ item.value.ttl }}


; AUTHORITY
@ SOA {{ item.value.primary }}. {{ item.value.admin|replace('@', '.') }}. (
      {{ item.value.serial }} ; serial
      1h         ; refresh
      15m        ; retry
      2w         ; expire
      1h )       ; minimum/negative


; NAMESERVERS
{{ item.value.nameservers }}


; MAILSERVERS
{{ item.value.mailservers }}


; HOSTS
{{ item.value.hosts }}


; CRYPTO
{{ item.value.crypto }}


; MISC
{{ item.value.crypto }}
